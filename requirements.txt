# Pinned to the `duo` conda environment (Python 3.12.13, CUDA 12.6).
#
#   conda create -n duo python=3.12
#   conda activate duo
#   pip install -r requirements.txt
#   pip install flash_attn-2.7.3+cu12torch2.6cxx11abiTRUE-cp312-cp312-linux_x86_64.whl
#
# flash-attn is installed LAST, from the prebuilt local wheel, because building it
# from source requires torch to already be present and takes ~1h.
#
# For an exact byte-for-byte reproduction of the environment (all transitive deps),
# use requirements-lock.txt instead.

# --- PyTorch (CUDA 12.6 build) -----------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.6.0+cu126
torchvision==0.21.0+cu126
torchaudio==2.6.0+cu126
triton==3.2.0

# --- training framework -------------------------------------------------------
lightning==2.6.0
torchmetrics==1.6.1
xformers==0.0.29.post3

# --- config -------------------------------------------------------------------
hydra-core==1.3.2
omegaconf==2.3.0

# --- data / tokenization ------------------------------------------------------
datasets==2.15.0
transformers==4.38.2
tokenizers==0.15.2
huggingface_hub==0.36.2
hf_transfer==0.1.9
pyarrow==23.0.1
fsspec==2023.10.0
ocifs==1.3.4
h5py==3.10.0

# --- numerics -----------------------------------------------------------------
numpy==1.26.4
scipy==1.17.1
pandas==2.2.1
scikit-learn==1.4.0
einops==0.7.0
timm==1.0.26

# --- logging / plotting / dev -------------------------------------------------
wandb==0.25.1
rich==13.7.1
matplotlib==3.10.8
seaborn==0.13.2
notebook==7.1.1
ipdb==0.13.13
nvitop==1.3.2
packaging==24.0
requests==2.33.1
